<div class="container">
    <h2> Show Composers</h2>
    <table>
        <tr>
            <th>Lastname</th>
            <th>Firstname</th>
            <th>Date of birth</th>
            <th>Place of birth</th>
            <th>Birth Country</th>
            <th>Deceaed</th>
        </tr>
    <?php

        if (array_key_exists('composers',$this->vars)) {
            foreach ($this->vars['composers'] as $composer) {
                ?>
                <tr>
                    <td>
                        <a href="?action=editComposer&id=<?= $composer->getID(); ?>" class="edit">
                            <?= $composer->getComposerFirstname(); ?></td>
                        </a>
                    <td class="override">
                        <a href="?action=editComposer&id=<?= $composer->getID(); ?>" class="edit">
                            <?= $composer->getComposerLastname(); ?>
                        </a>
                    </td>

                    <td><?= $composer->getDateOfBirth(); ?></td>
                    <td><?= $composer->getPlaceOfBirth(); ?></td>
                    <td><?= $composer->getBirthCountry(); ?></td>
                    <td><?= $composer->getDeceased(); ?></td>
                </tr>
                <?php
            }
        }
    ?>
    </table>
</div>